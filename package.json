{
  "name": "snake-game",
  "version": "1.0.0",
  "private": true,
  "description": "Serpent — Neon Snake game",
  "license": "ISC",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "jsdom": "^25.0.1",
    "vitest": "^3.2.4"
  }
}
